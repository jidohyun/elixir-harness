# Spec Directory

Specs mirror the Elixir module namespace.

Examples:

```text
spec/app/example_context.spec.md
spec/app/example_context/example_repository.spec.md
spec/app_web/example_live/index.spec.md
```

Use the templates in `templates/` as starting points.
